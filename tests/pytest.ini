[pytest]

# disable deprecation warnings, because the warnings about numpy.asscalar being
# deprecated make the test suite hang up and crash during printing the warnings
# summary.
filterwarnings =
    ignore::DeprecationWarning
